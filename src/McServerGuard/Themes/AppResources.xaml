<!-- 🎨 AppResources.xaml —— Material Design 深度定制资源字典 -->
<!-- 在这里定义所有自定义颜色、笔刷、导航样式和卡片样式 -->
<!-- 统一管理视觉风格，改一个地方全家桶都变色 🌈 -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- ✨ 统一动画资源 —— 全项目共用，改一处全局生效 -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <!-- 🎢 标准缓动函数 -->
    <CubicEase x:Key="StandardEase" EasingMode="EaseOut" />
    <QuarticEase x:Key="EmphasizedEase" EasingMode="EaseOut" />

    <!-- 📄 页面入场故事板：淡入 + 从右滑入 -->
    <Storyboard x:Key="PageEnterStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1"
                         Duration="0:0:0.3"
                         EasingFunction="{StaticResource StandardEase}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                         From="30" To="0"
                         Duration="0:0:0.35"
                         EasingFunction="{StaticResource EmphasizedEase}" />
    </Storyboard>

    <!-- 🃏 动画卡片样式：悬停上浮 + 阴影加深 -->
    <Style x:Key="AnimatedCardStyle" TargetType="materialDesign:Card">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="UniformCornerRadius" Value="12" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0" />
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#000000" Opacity="0" BlurRadius="0" ShadowDepth="0" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                             Duration="0:0:0.2"
                                             To="-3"
                                             EasingFunction="{StaticResource StandardEase}" />
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                             Duration="0:0:0.25"
                                             To="0.25"
                                             EasingFunction="{StaticResource StandardEase}" />
                            <DoubleAnimation Storyboard.TargetProperty="Effect.BlurRadius"
                                             Duration="0:0:0.25"
                                             To="12"
                                             EasingFunction="{StaticResource StandardEase}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                             Duration="0:0:0.2"
                                             To="0"
                                             EasingFunction="{StaticResource StandardEase}" />
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                             Duration="0:0:0.2"
                                             To="0"
                                             EasingFunction="{StaticResource StandardEase}" />
                            <DoubleAnimation Storyboard.TargetProperty="Effect.BlurRadius"
                                             Duration="0:0:0.2"
                                             To="0"
                                             EasingFunction="{StaticResource StandardEase}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 🔘 动画按钮样式：点击缩放反馈 -->
    <Style x:Key="AnimatedButtonStyle" TargetType="Button"
           BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                             Duration="0:0:0.08"
                                             To="0.96"
                                             EasingFunction="{StaticResource StandardEase}" />
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                             Duration="0:0:0.08"
                                             To="0.96"
                                             EasingFunction="{StaticResource StandardEase}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                             Duration="0:0:0.15"
                                             To="1"
                                             EasingFunction="{StaticResource EmphasizedEase}" />
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                             Duration="0:0:0.15"
                                             To="1"
                                             EasingFunction="{StaticResource EmphasizedEase}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- 🌈 自定义笔刷 —— 苍穹蓝 + 水蓝色，清新科技感 -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <!-- ✏️ AppFontFamily: 全局字体族，运行时由 ThemeService 覆盖 -->
    <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>

    <!-- 🟦 AccentGradientBrush: 苍穹蓝 → 水蓝 渐变，用于标题栏、按钮等高亮区域 -->
    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#60A5FA" Offset="0" />
        <GradientStop Color="#3B82F6" Offset="1" />
    </LinearGradientBrush>

    <!-- 🃏 CardBackgroundBrush: 深邃蓝卡片背景 -->
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#0F172A" />

    <!-- 🃏 CardHoverBrush: 卡片悬停色，比背景亮一丢丢 -->
    <SolidColorBrush x:Key="CardHoverBrush" Color="#1E293B" />

    <!-- 🧭 NavItemSelectedBrush: 导航项选中背景，苍穹蓝 -->
    <SolidColorBrush x:Key="NavItemSelectedBrush" Color="#3B82F6" />

    <!-- 🧭 NavItemHoverBrush: 导航项悬停背景，深蓝灰 -->
    <SolidColorBrush x:Key="NavItemHoverBrush" Color="#1E3A5F" />

    <!-- 🟢 GaugeGreenBrush: 仪表盘绿色 —— 一切正常 🟢 -->
    <SolidColorBrush x:Key="GaugeGreenBrush" Color="#4CAF50" />

    <!-- 🟡 GaugeYellowBrush: 仪表盘黄色 —— 要注意了 🟡 -->
    <SolidColorBrush x:Key="GaugeYellowBrush" Color="#FFC107" />

    <!-- 🔴 GaugeRedBrush: 仪表盘红色 —— 危险危险！🔴 -->
    <SolidColorBrush x:Key="GaugeRedBrush" Color="#F4364C" />

    <!-- 🧡 AccentTextBrush: 强调文字色，暖珊瑚 -->
    <SolidColorBrush x:Key="AccentTextBrush" Color="#FB7185" />

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- 🆕 补齐语义颜色资源键 —— 让主题色覆盖到全项目 -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <!-- 🎯 SubtleBorderBrush: 柔和边框色（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="SubtleBorderBrush" Color="#334155" />

    <!-- 🎯 DeepBackgroundBrush: 更深的背景色（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="DeepBackgroundBrush" Color="#020617" />

    <!-- 🎯 CardSubtleBorderBrush: 卡片半透明边框（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="CardSubtleBorderBrush" Color="#331E293B" />

    <!-- 🎯 PrimarySubtleBorderBrush: 主色半透明边框（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="PrimarySubtleBorderBrush" Color="#333B82F6" />

    <!-- 🎯 AccentSubtleBorderBrush: 强调色半透明边框（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="AccentSubtleBorderBrush" Color="#33FB7185" />

    <!-- 🎯 LoadingOverlayBrush: 加载遮罩半透明背景（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="LoadingOverlayBrush" Color="#CC0F172A" />

    <!-- 🎯 TerminalBackgroundBrush: 终端深色背景（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="TerminalBackgroundBrush" Color="#0C1222" />

    <!-- 🎯 SuccessInputForegroundBrush: 成功/终端水蓝前景 -->
    <SolidColorBrush x:Key="SuccessInputForegroundBrush" Color="#7DD3FC" />

    <!-- 🎯 InfoInputForegroundBrush: 信息/输入淡蓝前景 -->
    <SolidColorBrush x:Key="InfoInputForegroundBrush" Color="#94A3B8" />

    <!-- 🎯 ErrorTextBrush: 错误文字珊瑚红 -->
    <SolidColorBrush x:Key="ErrorTextBrush" Color="#FB7185" />

    <!-- 🎯 NavItemSelectedIndicatorBrush: 导航选中竖线指示器（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="NavItemSelectedIndicatorBrush" Color="#3B82F6" />

    <!-- 🎯 NavItemSelectedHoverBrush: 导航选中+悬停叠加（ThemeService 运行时覆盖） -->
    <SolidColorBrush x:Key="NavItemSelectedHoverBrush" Color="#2563EB" />

    <!-- 🆔 SwatchHoverBorderBrush: 色板悬停白边框（原硬编码 #FFFFFF） -->
    <SolidColorBrush x:Key="SwatchHoverBorderBrush" Color="#FFFFFF" />

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- 🧭 ModernNavListBoxStyle: 现代化侧边导航样式 -->
    <!-- 选中状态带左侧发光竖线 + 背景渐变微光，悬停缩放 + 图标颜色过渡 -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <!-- 🎯 现代导航项 ItemContainerStyle：每个 ListBoxItem 的外观 -->
    <Style x:Key="ModernNavListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="Margin" Value="6,3" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True" />
                        <Border x:Name="HoverBg"
                                Background="{DynamicResource NavItemHoverBrush}"
                                CornerRadius="10"
                                Opacity="0"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <BlurEffect Radius="8" />
                            </Border.Effect>
                        </Border>
                        <Border x:Name="SelectedBg"
                                CornerRadius="10"
                                Opacity="0"
                                SnapsToDevicePixels="True"
                                Background="#1F3B82F6" />
                        <Border x:Name="SelectionIndicator"
                                HorizontalAlignment="Left"
                                Width="4"
                                CornerRadius="2"
                                Margin="-2,0,0,0"
                                Background="{DynamicResource NavItemSelectedIndicatorBrush}"
                                Opacity="0"
                                VerticalAlignment="Stretch">
                            <Border.Effect>
                                <DropShadowEffect Color="#3B82F6"
                                                  BlurRadius="6"
                                                  ShadowDepth="0"
                                                  Opacity="0.6" />
                            </Border.Effect>
                        </Border>
                        <Border Padding="{TemplateBinding Padding}"
                                x:Name="ContentBorder"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <ContentPresenter Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Stretch" />
                        </Border>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBg"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.25"
                                                         To="1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ContentBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         Duration="0:0:0.2"
                                                         To="1.02">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ContentBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         Duration="0:0:0.2"
                                                         To="1.02">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected" />
                                <VisualState x:Name="Selected">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SelectedBg"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.3"
                                                         To="1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="SelectionIndicator"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.3"
                                                         To="1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="SelectedUnfocused">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SelectedBg"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.3"
                                                         To="1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="SelectionIndicator"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.3"
                                                         To="1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 📋 ModernNavListBoxStyle: ListBox 整体样式 -->
    <Style x:Key="ModernNavListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource ModernNavListBoxItemStyle}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Margin" Value="8,8" />
        <Setter Property="Padding" Value="0,4" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- 🧭 NavListBoxStyle: 侧边抽屉导航列表样式（兼容旧版） -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <Style x:Key="NavListBoxItemStyle" TargetType="ListBoxItem"
           BasedOn="{StaticResource ModernNavListBoxItemStyle}" />
    <Style x:Key="NavListBoxStyle" TargetType="ListBox"
           BasedOn="{StaticResource ModernNavListBoxStyle}" />

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- 🃏 EnhancedCardStyle: 增强卡片样式 -->
    <!-- 圆角 12px，加深阴影，悬停时微微变亮 + 轻微上浮 -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <Style x:Key="EnhancedCardStyle" TargetType="materialDesign:Card">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
        <Setter Property="Margin" Value="4" />
        <!-- 📐 圆角 12px，比默认的更圆润 -->
        <Setter Property="UniformCornerRadius" Value="12" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- 🖱️ 悬停效果 -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource CardHoverBrush}" />
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                             Duration="0:0:0.2"
                                             To="-2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                             Duration="0:0:0.2"
                                             To="0">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- ⚙️ 设置页面样式 -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <!-- 🎯 CardBorderStyle: 设置卡片边框样式 -->
    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="0,0,0,16" />
        <Setter Property="BorderBrush" Value="{DynamicResource CardSubtleBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!-- 🎯 SectionTitleStyle: 设置区域标题 -->
    <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <!-- 🎯 SettingLabelStyle: 设置项标签 -->
    <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="Margin" Value="0,0,0,6" />
    </Style>

    <!-- 🎯 SettingValueStyle: 设置值显示 -->
    <Style x:Key="SettingValueStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
        <Setter Property="Margin" Value="8,0,0,0" />
    </Style>

    <!-- 🎯 ColorSwatchStyle: 颜色预设按钮样式 -->
    <Style x:Key="ColorSwatchStyle" TargetType="Button">
        <Setter Property="Width" Value="28" />
        <Setter Property="Height" Value="28" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect x:Name="Shadow" Color="#000000" Opacity="0" BlurRadius="8" ShadowDepth="0" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <Border x:Name="HoverBorder"
                                Background="Transparent"
                                BorderBrush="{DynamicResource SwatchHoverBorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                Opacity="0"
                                IsHitTestVisible="False" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.2"
                                                         To="1">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="Shadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.2"
                                                         To="0.5">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.15"
                                                         To="1" />
                                        <DoubleAnimation Storyboard.TargetName="Shadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.15"
                                                         To="0.5" />
                                        <DoubleAnimation Storyboard.TargetName="Bd"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                         Duration="0:0:0.15"
                                                         To="0.9">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="Bd"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                         Duration="0:0:0.15"
                                                         To="0.9">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
