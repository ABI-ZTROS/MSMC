# 模组端核心配置翻译验收与收尾计划

## 摘要

本计划针对 MSMC（McServerGuard）项目「模组端 4 核心（Forge / NeoForge / Fabric / Quilt）配置文件中文翻译」任务进行最终验收与收尾。经 Phase 1 探索确认，任务的 8 个交付物（4 份 Markdown 文档 + 4 份 C# 注册方法代码片段）均已存在且质量良好，仅存在 1 处轻微的一致性缺口需要补齐，最后需要产出用户要求的「文件路径 + 配置项数量统计」最终报告。

## 当前状态分析

### 已交付物（全部存在，已逐字核对）

| # | 文件路径 | 状态 | 备注 |
|---|---|---|---|
| 1 | `/workspace/docs/server-cores/18-forge.md` | ✅ 完整 | 含核心简介、配置清单、forge-server.toml（5 项）+ forge-common.toml（1 项）翻译表、配置示例、优化建议、参考链接 |
| 2 | `/workspace/docs/server-cores/19-neoforge.md` | ✅ 完整 | 含核心简介、配置清单、neoforge-server.toml（5 项）+ neoforge-common.toml（3 项）+ neoforge-client.toml（4 项，仅作了解）翻译表、配置示例、优化建议、参考链接 |
| 3 | `/workspace/docs/server-cores/20-fabric.md` | ✅ 完整 | 含核心简介、配置清单、fabric-server-launcher.properties（1 项）翻译表、配置示例、启动方式、优化建议、参考链接 |
| 4 | `/workspace/docs/server-cores/21-quilt.md` | ✅ 完整 | 含核心简介、配置清单、quilt-server-launcher.properties（1 项）翻译表、配置示例、启动方式、优化建议、参考链接 |
| 5 | `/workspace/docs/server-cores/_patches/RegisterForgeServerToml.cs` | ⚠️ 缺 1 项 | 仅注册了 forge-server.toml 的 5 个 [server] 节条目；**缺失** forge-common.toml 的 `logLegacyTagWarnings` 条目（该条目在 18-forge.md 中已有文档，且 NeoForge 的对应 patch 已包含 common 条目） |
| 6 | `/workspace/docs/server-cores/_patches/RegisterNeoForgeYml.cs` | ✅ 完整 | 注册 neoforge-server.toml 5 项 + neoforge-common.toml 3 项，共 8 项 |
| 7 | `/workspace/docs/server-cores/_patches/RegisterFabricServerProperties.cs` | ✅ 完整 | 注册 fabric-server-launcher.properties 1 项 |
| 8 | `/workspace/docs/server-cores/_patches/RegisterQuiltServerProperties.cs` | ✅ 完整 | 注册 quilt-server-launcher.properties 1 项 |

### 模式合规性验证

- **ServerConfigDescriptor 字段名**：所有 4 个 C# patch 使用的字段（`Key` / `ConfigFileName` / `DisplayName` / `Description` / `Category` / `DefaultValue` / `AllowedValues` / `ValueType` / `RequiresRestart`）均与 `/workspace/src/McServerGuard/Services/ConfigManagement/ConfigDescriptorRegistry.cs` 第 22-58 行的类定义完全匹配，全部为合法字段。
- **代码风格**：与同目录已有 `_patches/RegisterNukkitYml.cs` 的写法一致（私有方法 + `const string file` + 多个 `Register(new ServerConfigDescriptor { ... })`）。
- **Markdown 格式**：与参考文档 `06-purpur.md` / `07-pufferfish.md` 一致（核心简介、配置文件清单表、阅读约定、分节翻译表、配置示例、优化建议、参考链接、免责声明）。
- **翻译规范**：键名不翻译 ✅ / 枚举值翻译 ✅ / 值类型标注 ✅ / 取值范围明确 ✅ / 重启标注（✅/🔄）✅ / 说明详尽 ✅。

### 已知缺口（仅 1 处）

`RegisterForgeServerToml.cs` 中未注册 `forge-common.toml` 的 `general.logLegacyTagWarnings` 条目。该条目：
- 在 `18-forge.md` 第 67-69 行已完整文档化（含枚举值 `OFF`/`ONLY_IN_DEV_ENV`/`ALWAYS` 翻译）。
- 在 NeoForge 的对应 patch（`RegisterNeoForgeYml.cs` 第 85-128 行）中已为 `neoforge-common.toml` 注册了 3 个 common 条目作为平行实现。

为保持 Forge 与 NeoForge 两套 patch 的一致性（两者都应覆盖各自 markdown 中文档化的全部条目），需要补齐这 1 个条目。

## 拟议变更

### 变更 1：补齐 RegisterForgeServerToml.cs 的 forge-common.toml 条目

- **文件**：`/workspace/docs/server-cores/_patches/RegisterNeoForgeYml.cs` 的同级文件 `/workspace/docs/server-cores/_patches/RegisterForgeServerToml.cs`
- **做什么**：在文件末尾（`}` 闭合大括号前）追加 1 个 `Register(new ServerConfigDescriptor { ... })` 块，注册 `general.logLegacyTagWarnings` 条目，对应 `forge-common.toml`。
- **为什么**：补齐与 18-forge.md 文档及 NeoForge patch 的对称性，让 Forge 的代码片段覆盖其文档中列出的全部 6 个条目（而非当前的 5 个）。
- **怎么做**：
  - 新增 `const string commonFile = "forge-common.toml";`
  - 注册条目：`Key = "general.logLegacyTagWarnings"`，`ConfigFileName = commonFile`，`DisplayName = "旧标签迁移助手模式"`，`ValueType = "enum"`，`AllowedValues = ["OFF", "ONLY_IN_DEV_ENV", "ALWAYS"]`，`DefaultValue = "OFF"`，`RequiresRestart = false`。
  - Description 文案与 18-forge.md 第 69 行保持一致（含枚举值中文翻译）。
- **影响范围**：仅修改 1 个 patch 文件，不影响其他文件、不影响主项目 `ConfigDescriptorRegistry.cs`（patch 文件本就是待粘贴的片段，未集成进主注册表）。

### 变更 2：产出最终统计报告

- **做什么**：在最终回复中列出全部 8 个产出文件的绝对路径，以及每个核心的配置项数量统计（区分「Markdown 文档化条目数」与「C# patch 注册条目数」）。
- **为什么**：用户最后一条消息明确要求「完成后，列出所有产出文件路径和每个核心的配置项数量统计」。

## 假设与决策

1. **不集成进主注册表**：与 `_patches/RegisterNukkitYml.cs` 一致，本任务的 4 个 patch 文件保留为独立片段，**不**修改 `ConfigDescriptorRegistry.cs` 主文件。集成工作属于计划文档「阶段二：代码集成」的后续任务，不在本次收尾范围内。
2. **Forge common 条目枚举值**：Forge 1.21.x 的 `logLegacyTagWarnings` 枚举为 `OFF`/`ONLY_IN_DEV_ENV`/`ALWAYS`（与 NeoForge 的 `SILENCED`/`DEV_SHORT`/`DEV_LONG`/`ENABLED` 不同），严格按 18-forge.md 已文档化的值填写，不照搬 NeoForge。
3. **不修改 Markdown**：4 份 Markdown 文档已完整且准确，本次不做任何改动。
4. **不动其他 patch 文件**：NeoForge / Fabric / Quilt 的 3 个 patch 文件已完整，本次不动。

## 验证步骤

1. **文件存在性**：变更后再次 `ls` 确认 8 个文件齐备（已通过 Phase 1 验证）。
2. **字段合法性**：新增的 `general.logLegacyTagWarnings` 块字段名与 `ServerConfigDescriptor` 类定义一致（已通过 Grep 验证类定义）。
3. **对称性**：Forge patch 注册条目数（6）与 18-forge.md 文档化条目数（6）一致；与 NeoForge patch（8 项，含 common）形成平行结构。
4. **统计准确性**：最终报告的配置项计数基于实际 `Register(new ServerConfigDescriptor` 出现次数，可复核。

## 预期最终统计（变更后）

| 核心 | Markdown 文档 | C# Patch 文件 | Markdown 文档化条目数 | C# 注册条目数 |
|---|---|---|---|---|
| Forge | 18-forge.md | RegisterForgeServerToml.cs | 6 | 6（变更后） |
| NeoForge | 19-neoforge.md | RegisterNeoForgeYml.cs | 8（+4 client 仅作了解） | 8 |
| Fabric | 20-fabric.md | RegisterFabricServerProperties.cs | 1 | 1 |
| Quilt | 21-quilt.md | RegisterQuiltServerProperties.cs | 1 | 1 |
| **合计** | 4 份 | 4 份 | 16（+4 client） | 16 |
