# 全面性能优化计划 · 动画流畅 + 配置异步化

## 一、问题诊断总结

经全面代码审查，性能问题分三大类：

### A. 动画卡顿元凶（用户最关心）
| # | 问题 | 文件:行号 | 严重度 |
|---|------|-----------|--------|
| A1 | `AnimatedCardStyle` 动画化 `Effect.BlurRadius`（每帧软件光栅化） | `Themes/AppResources.xaml:65-68,83-86` | 高 |
| A2 | 导航项 `HoverBg` 常驻 `BlurEffect Radius=8` | `Themes/AppResources.xaml:256` | 高 |
| A3 | 配置卡片 100+ 路并发入场动画（Loaded 触发） | `ConfigEditorPage.xaml:36-49` | 高 |
| A4 | 配置列表无虚拟化，200 项全量实例化 | `ConfigEditorPage.xaml:478-618` | 高 |

### B. 配置读写阻塞 UI 线程（用户最关心）
| # | 问题 | 文件:行号 | 严重度 |
|---|------|-----------|--------|
| B1 | `ReadConfigAsync` 解析在 UI 线程，无 ConfigureAwait(false) | `ConfigManager.cs:67-78` | 高 |
| B2 | `SaveConfigAsync` 序列化在 UI 线程 | `ConfigManager.cs:104-132` | 高 |
| B3 | 文件读写未用 FileShare.ReadWrite | `ConfigManager.cs:67,132` | 中 |
| B4 | `LoadConfigAsync` 批量加载节奏差 + 进度通知过频 | `ConfigEditorViewModel.cs:838-862` | 高 |
| B5 | `ScanDirectoryForConfigFiles` 同步递归扫描在 UI 线程 | `ConfigEditorViewModel.cs:628-662` | 中 |

### C. 其他性能问题
| # | 问题 | 文件:行号 | 严重度 |
|---|------|-----------|--------|
| C1 | `SystemMonitor.CollectSnapshot` 同步 WMI 阻塞 | `SystemMonitor.cs:108-164` | 高 |
| C2 | `MemoryOptimizerService` 在 UI 线程做 GC.Collect | `MemoryOptimizerService.cs:151` | 中 |
| C3 | `OnConfigEntryChanged` 每按键 O(n) 扫描 | `ConfigEditorViewModel.cs:906-918` | 中 |
| C4 | `UpdateGroupedEntries` 用 Dispatcher.Invoke 同步阻塞 | `ConfigEditorViewModel.cs:564-573` | 中 |

---

## 二、涉及文件

| 文件 | 改动范围 |
|------|----------|
| `src/McServerGuard/Themes/AppResources.xaml` | A1, A2：移除 BlurEffect 动画 |
| `src/McServerGuard/Views/ConfigEditorPage.xaml` | A3, A4：虚拟化 + 动画优化 |
| `src/McServerGuard/Services/ConfigManagement/ConfigManager.cs` | B1, B2, B3：异步化 + FileShare |
| `src/McServerGuard/ViewModels/ConfigEditorViewModel.cs` | B4, B5, C3, C4：加载优化 |
| `src/McServerGuard/Services/SystemMonitoring/SystemMonitor.cs` | C1：异步采集 |
| `src/McServerGuard/Services/MemoryOptimizerService.cs` | C2：GC 移到线程池 |

---

## 三、具体改动方案

### 改动 A1：移除 AnimatedCardStyle 的 BlurRadius 动画

**文件**：`Themes/AppResources.xaml`，Line 47-91

**问题**：动画化 `Effect.BlurRadius` 每帧触发软件光栅化，是动画卡顿的主要元凶。

**方案**：保留 `RenderTransform.Y` 位移动画（GPU 友好），移除 `Effect.Opacity` 和 `Effect.BlurRadius` 的动画。将 DropShadowEffect 改为静态值（悬停时直接显示，不做动画过渡）：

```xml
<!-- 修改后的 AnimatedCardStyle -->
<Setter Property="Effect">
    <Setter.Value>
        <DropShadowEffect Color="#000000" Opacity="0.25" BlurRadius="12" ShadowDepth="0" />
    </Setter.Value>
</Setter>
<Style.Triggers>
    <Trigger Property="IsMouseOver" Value="True">
        <Trigger.EnterActions>
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                     Duration="0:0:0.2" To="-3"
                                     EasingFunction="{StaticResource StandardEase}" />
                </Storyboard>
            </BeginStoryboard>
        </Trigger.EnterActions>
        <Trigger.ExitActions>
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                     Duration="0:0:0.2" To="0"
                                     EasingFunction="{StaticResource StandardEase}" />
                </Storyboard>
            </BeginStoryboard>
        </Trigger.ExitActions>
    </Trigger>
</Style.Triggers>
```

### 改动 A2：移除导航项 HoverBg 的 BlurEffect

**文件**：`Themes/AppResources.xaml`，Line 256 附近

**方案**：删除 `HoverBg` 元素上的 `<BlurEffect Radius="8"/>`，改用预渲染的径向渐变模拟光晕效果：

```xml
<!-- 将 BlurEffect 替换为静态渐变背景 -->
<Border x:Name="HoverBg" ...>
    <Border.Background>
        <RadialGradientBrush>
            <GradientStop Color="{DynamicResource PrimaryHueMidBrush}" Offset="0" />
            <GradientStop Color="Transparent" Offset="1" />
        </RadialGradientBrush>
    </Border.Background>
    <!-- 删除 <Border.Effect><BlurEffect Radius="8"/></Border.Effect> -->
</Border>
```

### 改动 A3：配置卡片入场动画改为仅首屏触发

**文件**：`ConfigEditorPage.xaml`，Line 36-49（`ConfigItemCardStyle`）

**问题**：100+ 卡片同时触发 Loaded 动画，与批量 Add 叠加导致 Composition 线程过载。

**方案**：将入场动画时长从 0.25s/0.3s 缩短到 0.15s/0.15s，并降低并发压力。移除 `TranslateTransform.Y` 动画（布局推动成本高），仅保留 Opacity 淡入：

```xml
<Style.Triggers>
    <EventTrigger RoutedEvent="Loaded">
        <BeginStoryboard>
            <Storyboard>
                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)"
                                 From="0" To="1"
                                 Duration="0:0:0.15"
                                 EasingFunction="{StaticResource ConfigCardEase}" />
            </Storyboard>
        </BeginStoryboard>
    </EventTrigger>
</Style.Triggers>
```

### 改动 A4：配置列表启用虚拟化

**文件**：`ConfigEditorPage.xaml`，Line 478-618

**问题**：`ItemsControl` 默认不虚拟化，200 项全量实例化。

**方案**：外层 ItemsControl 添加虚拟化面板 + ScrollViewer 启用延迟滚动：

```xml
<ScrollViewer Grid.Row="1"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Disabled"
              IsDeferredScrollingEnabled="True"
              CanContentScroll="True">
    <ItemsControl ItemsSource="{Binding GroupedConfigEntries}"
                  Background="Transparent"
                  BorderThickness="0"
                  Padding="0"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <VirtualizingStackPanel IsVirtualizing="True" VirtualizationMode="Recycling" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <!-- 其余 ItemTemplate 不变 -->
    </ItemsControl>
</ScrollViewer>
```

### 改动 B1+B2+B3：ConfigManager 真正异步化

**文件**：`ConfigManager.cs`，Line 54-136

**方案**：文件 IO 用 FileStream + FileShare.ReadWrite，解析/序列化用 Task.Run 包装，全部加 ConfigureAwait(false)：

```csharp
public async Task<Dictionary<string, string>> ReadConfigAsync(string filePath)
{
    ArgumentNullException.ThrowIfNull(filePath);
    Log.Information("📂 读取配置文件: {Path}", filePath);
    if (!File.Exists(filePath))
        throw new FileNotFoundException($"配置文件不存在: {filePath}", filePath);

    // 用 FileShare.ReadWrite 避免与服务器进程冲突
    string content;
    using (var fs = new FileStream(filePath, FileMode.Open, FileAccess.Read, FileShare.ReadWrite))
    using (var reader = new StreamReader(fs))
    {
        content = await reader.ReadToEndAsync().ConfigureAwait(false);
    }

    var extension = Path.GetExtension(filePath).ToLowerInvariant();
    var format = ConfigFormatDetector.Resolve(content, extension);

    // CPU 密集型解析放到线程池
    var result = await Task.Run(() => format switch
    {
        ConfigFormat.Properties => ParseProperties(content),
        ConfigFormat.Yaml => FlattenYaml(content),
        ConfigFormat.Json => FlattenJson(content),
        _ => throw HandleUnsupportedFormat(extension)
    }).ConfigureAwait(false);

    Log.Information("✅ 配置解析完成，共 {Count} 个键值对", result.Count);
    return result;
}

public async Task SaveConfigAsync(string filePath, Dictionary<string, string> config)
{
    ArgumentNullException.ThrowIfNull(filePath);
    ArgumentNullException.ThrowIfNull(config);
    Log.Information("💾 保存配置到: {Path}", filePath);

    var extension = Path.GetExtension(filePath).ToLowerInvariant();
    var format = ConfigFormatDetector.DetectByExtension(extension);

    // CPU 密集型序列化放到线程池
    var content = await Task.Run(() => format switch
    {
        ConfigFormat.Properties => PropertiesParser.Serialize(config),
        ConfigFormat.Yaml => SerializeYaml(config),
        ConfigFormat.Json => SerializeJson(config),
        _ => throw new NotSupportedException($"不支持的配置文件格式: {extension}")
    }).ConfigureAwait(false);

    var directory = Path.GetDirectoryName(filePath);
    if (directory is not null && !Directory.Exists(directory))
        Directory.CreateDirectory(directory);

    // 用 FileShare.ReadWrite 写入
    using (var fs = new FileStream(filePath, FileMode.Create, FileAccess.Write, FileShare.ReadWrite))
    using (var writer = new StreamWriter(fs))
    {
        await writer.WriteAsync(content).ConfigureAwait(false);
    }

    Log.Information("配置文件 {FilePath} 已保存，共 {Count} 个配置项 ✅", filePath, config.Count);
}
```

### 改动 B4：LoadConfigAsync 批量优化

**文件**：`ConfigEditorViewModel.cs`，Line 838-862

**方案**：
1. 进度更新移到每批结束（而非每个 entry）
2. `Task.Delay(8)` 改为 `Task.Yield()`（让 UI 自然调度，不强制延迟）
3. Clear 时取消旧事件订阅

```csharp
// 在 ConfigEntries.Clear() 前，取消旧 entry 的事件订阅
foreach (var oldEntry in ConfigEntries)
{
    oldEntry.PropertyChanging -= OnConfigEntryChanging;
    oldEntry.PropertyChanged -= OnConfigEntryChanged;
}
ConfigEntries.Clear();
_originalConfig = new Dictionary<string, string>(config);
HasUnsavedChanges = false;
_undoStack.Clear();
UndoCommand.NotifyCanExecuteChanged();

const int batchSize = 10;  // 从 5 提高到 10
int total = processedEntries.Count;
int processed = 0;

for (int i = 0; i < total; i += batchSize)
{
    if (cancellationToken.IsCancellationRequested) return;

    var batch = processedEntries.Skip(i).Take(batchSize).ToList();
    foreach (var entry in batch)
    {
        entry.PropertyChanging += OnConfigEntryChanging;
        entry.PropertyChanged += OnConfigEntryChanged;
        ConfigEntries.Add(entry);
        processed++;
    }
    // 每批结束才更新进度
    LoadProgress = (int)(processed * 100.0 / total);

    await Task.Yield();  // 让 UI 线程喘息，但不强制延迟
}
```

### 改动 B5：ScanDirectoryForConfigFiles 异步化

**文件**：`ConfigEditorViewModel.cs`，Line 628-662

**方案**：改为 `async Task`，用 `Task.Run` 包装递归扫描，完成后在 UI 线程更新属性：

```csharp
private async Task ScanDirectoryForConfigFilesAsync(string rootPath)
{
    if (!Directory.Exists(rootPath))
    {
        ConfigFiles = [];
        ConfigFileTree = [];
        return;
    }

    Log.Information("🔍 递归扫描配置文件目录: {Path}", rootPath);

    var (flatList, treeRoot) = await Task.Run(() =>
    {
        var flat = new List<string>();
        var tree = new List<ConfigFileItem>();
        var supportedExtensions = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
        {
            ".properties", ".yml", ".yaml", ".json", ".cfg", ".conf",
            ".toml", ".ini", ".txt"
        };
        try
        {
            BuildConfigFileTree(rootPath, rootPath, supportedExtensions, tree, flat, 0);
        }
        catch (Exception ex)
        {
            Log.Error(ex, "扫描配置文件目录失败: {Message}", ex.Message);
        }
        return (flat, tree);
    }).ConfigureAwait(true);  // 回到 UI 线程更新属性

    ConfigFiles = flatList;
    ConfigFileTree = treeRoot;
    OnPropertyChanged(nameof(ConfigFileCountText));
    Log.Information("✅ 扫描完成，找到 {Count} 个配置文件", flatList.Count);
}
```

同时修改 `OnServerChanged` 中的调用点（Line 510）和 `RescanConfigFiles`（Line 350）改为 `await ScanDirectoryForConfigFilesAsync(...)`。

### 改动 C1：SystemMonitor 异步采集

**文件**：`SystemMonitor.cs`，Line 108-164

**方案**：新增 `CollectSnapshotAsync` 方法，将 WMI/PerformanceCounter 调用包装到 Task.Run，原 `CollectSnapshot` 保留为同步版本供内部调用：

```csharp
/// <summary>
/// 异步采集系统指标快照 —— 将 WMI/PerformanceCounter 调用放到线程池
/// </summary>
public async Task<SystemMetrics> CollectSnapshotAsync()
{
    return await Task.Run(() => CollectSnapshot()).ConfigureAwait(false);
}
```

然后修改定时器回调（Line 228 附近）调用 `CollectSnapshotAsync` 而非 `CollectSnapshot`。

### 改动 C2：MemoryOptimizerService GC 移到线程池

**文件**：`MemoryOptimizerService.cs`，Line 151 附近

**方案**：将 `GC.Collect` 调用包装到 `Task.Run`：

```csharp
// 原：DispatcherTimer 回调中直接 GC.Collect
// 改：DispatcherTimer 回调中 Task.Run 执行 GC
await Task.Run(() =>
{
    GC.Collect(2, GCCollectionMode.Optimized, false, false);
}).ConfigureAwait(false);
```

或将 `DispatcherTimer` 改为 `System.Threading.Timer`，整个回调在线程池执行。

### 改动 C3：OnConfigEntryChanged 优化 O(n) 扫描

**文件**：`ConfigEditorViewModel.cs`，Line 906-918

**方案**：用 `_modifiedCount` 计数器替代 `ConfigEntries.Any(...)`：

```csharp
// 新增字段
private int _modifiedCount;

// OnConfigEntryChanging 中：值改变前，如果当前是 modified 状态，计数器先减
// OnConfigEntryChanged 中：
private void OnConfigEntryChanged(object? sender, System.ComponentModel.PropertyChangedEventArgs e)
{
    if (sender is not ServerConfigEntry entry || e.PropertyName != nameof(ServerConfigEntry.Value))
        return;

    var wasModified = entry.IsModified;
    entry.IsModified = !string.Equals(entry.Value, entry.OriginalValue, StringComparison.Ordinal);
    
    if (entry.IsModified && !wasModified) _modifiedCount++;
    else if (!entry.IsModified && wasModified) _modifiedCount--;

    entry.IsValid = entry.Descriptor is null ||
                    _configManager.ValidateValue(entry.Key, entry.SourceFile, entry.Value);

    HasUnsavedChanges = _modifiedCount > 0;
    UndoCommand.NotifyCanExecuteChanged();
}
```

保存/重置/加载时重置 `_modifiedCount = 0`。

### 改动 C4：UpdateGroupedEntries 改为 BeginInvoke

**文件**：`ConfigEditorViewModel.cs`，Line 564-573

**方案**：`Dispatcher.Invoke` 改为 `BeginInvoke`，避免阻塞线程池线程：

```csharp
private void UpdateGroupedEntries()
{
    System.Windows.Application.Current?.Dispatcher?.BeginInvoke(new Action(() =>
    {
        GroupedConfigEntries = ConfigEntries
            .GroupBy(e => e.Descriptor?.Category ?? "其他")
            .Select(g => new ConfigEntryGroup(g.Key, g.ToList()))
            .ToList();
    }));
}
```

---

## 四、不改动项（已确认无问题）

- `AnimationHelper.cs`：规范的 DoubleAnimation + RenderTransform，无问题
- `PropertiesParser/YamlParser`：静态单例，实现正确
- `MainWindow` 页面切换动画：RenderTransform 驱动，GPU 友好
- `MainViewModel` 时钟 Timer：开销 <1ms，可忽略

---

## 五、验证步骤

1. **编译验证**：`dotnet build` 无错误
2. **动画流畅度验证**：
   - 悬停配置卡片：位移动画流畅，无明显卡顿
   - 悬停导航项：无 BlurEffect 导致的掉帧
   - 配置列表滚动：100+ 项流畅滚动，无卡顿
3. **配置加载验证**：
   - 打开 200 项的 paper-global.yml：加载期间 UI 不冻结，动画正常播放
   - 加载进度条更新平滑（每批更新一次）
4. **配置保存验证**：
   - 保存大配置文件：UI 不冻结，保存按钮有响应
   - 保存时文件被占用：正确提示错误
5. **系统监控验证**：
   - 监控运行期间 UI 无间歇卡顿
   - CPU/内存数值正常更新
6. **内存验证**：
   - 切换配置文件多次：内存无持续增长（事件订阅已取消）
