# MSMC 工程原则与 Agent 操作规程

> 用途：① 固化"三链原则"的精确定义；② 提炼项目级工程原则（来自历次审计）；③ 给出**针对廉价模型（doubaoseedcode 这类）的作战规则**——它们不立规矩就会跑偏、编造、越权。
> 适用：人审、子代理编排、任何 LLM 参与的代码生成/审计任务。

---

## 一、三链原则（核心方法论）

任何一行代码、任何一个操作，都必须同时追溯三条链。**缺一条，结论就不成立。**

### 1.1 执行链（Execution Chain）——「这条路真的走通了吗？」
- 这段代码**是不是真的被执行**：被调用了吗？被注册/订阅/打开了吗？
- `async/await` 结构是否正确：是否存在 `sync-over-async`（UI 线程 `.GetAwaiter().GetResult()` → 死锁）、`async void`（fire-and-forget 吞异常）、漏 `await`？
- 是否因早 `return` / 异常被吞而**跳过关键步骤**？（如：防火墙规则加不进去却继续往下走）
- 循环/递归是否会爆栈或死循环？定时器 `Stop` 后是否还在跑？
- 资源获取后，`Release/Dispose` **真的执行了吗**？

### 1.2 因果链（Causal Chain）——「它依赖的东西真的存在吗？」
- **注入的服务真的注册了吗？**（必须与 `App.xaml.cs` 的 DI 注册交叉验证，缺一即运行期崩溃）
- 配置键、路径、外部命令（`wmic/netstat/netsh/WMI/注册表`）**真的存在且可用吗**？
- Windows-only 假设是否成立？前置条件（端口空闲、权限、文件存在）是否满足？
- **跨组件契约一致性**：bridge 的 `action` 名、参数、返回值在 TS 端与 C# 端是否逐一对齐？（漂移即"点了没反应"）

### 1.3 返回链（Return Chain）——「返回值/异常语义和调用方想的一致吗？」
- 失败返回 `null`，但调用方直接解引用 → NRE？
- `bool` 语义是否歧义（`true`=成功 还是 `true`=已找到）？
- 异常被吞，导致调用方**误判成功**（这就是"假成功"病根）？
- 返回结构与声明类型是否匹配？

### 1.4 三链如何对抗"廉价模型造反"
廉价模型最擅长：**断言而不追溯**。三链原则强制它每一条结论都必须绑定"执行证据 + 依赖证据 + 返回语义证据"，从而把"它说修好了"变成"它证明修好了"。**不带三链的结论，一律视为未验证。**

---

## 二、项目级原则（从审计中提炼）

### P4 · 诚实返回链（最高优先级，源自🔴 P0 反复出现）
**绝不输出谎言字段。** 没有校验哈希，就不许出现 `HashVerified=true`；没有真正写入文件，就不许返回 `success:true`；`config:updateValue` 的 key 不存在，必须返回明确的错误而非静默成功。**失败必须可见**——这是 MSMC 的第一病根（静默失败），也是它最该根治的。

### P5 · 契约一致性
bridge 的 `action` 名、参数个数/顺序/类型、返回结构，TS↔C# 必须逐项对齐。新增契约必须配套**契约测试**，自动拦截 action 名漂移（历史已有 3 处"点了没反应"的断裂）。

### P6 · 异步纪律
不在 UI 线程做 `sync-over-async`；不用 `async void`（事件入口除外且必须 try/catch）；库代码一律 `ConfigureAwait(false)`；任何 fire-and-forget 必须走 `FireAndForgetTracker`（限并发、异常计数），禁止裸 `_ = Task.Run(...)`。

### P7 · 资源与 IO 诚信
- 文件写入原子化：`.part` → 校验通过 → `Move` → 失败 `Delete`，**绝不半截落地**。
- 禁止每方法 `new HttpClient`（Socket 泄漏，见 `PublicIpDetector`）；用单例 + `IHttpClientFactory`。
- 内存有界：大文件**流式哈希**（逐 64KB 块），不一次性读全；任何全量读文件必须评估 OOM 风险。

### P8 · 验证优先，不编造（源自 0d0488c 复审）
commit message / 报告 / 注释里**只写真正落地的实现**。声称"500ms 缓存""SharedMetricsSampler"但代码不存在，等同于撒谎。任何"已修复"断言必须附 `文件:行号` 证据，否则按未修复处理。

### P9 · 条件能力门卫
某项服务**条件注册**时（如 `CpuPowerService` 仅启用电源管理时），其所有访问点必须被同一开关守卫。禁止在门卫外 `GetRequiredService`，禁止无守卫地注入条件服务。

### P10 · 真机前模拟
上弱机（i3 / 4GB / 8MB/s）前，先做极端环境纸面演练：枚举每个交互元素→追踪完整链路→量化内存峰值/IO 量/耗时→判定 🔴/🟠/✅。模拟不过关，不许上真机。

### P11 · 防御纵深但不掩盖
启动装甲（三层异常 + ForceLog）是好的，但**运行时也必须诚实**。不要一边把崩溃路径焊死，一边让业务路径静默失败。装甲是兜底，不是遮羞布。

### P12 · 信任边界
C# 宿主可信，但任何来自 C# 的字符串在前端渲染前必须转义（防 XSS）。Origin 白名单必须校验**权威来源**（`e.Source`）而非消息自报字段。安全加固要真穿透，不要纸面。

---

## 三、针对廉价模型的作战规则（doubaoseedcode 等预算模型）

> 这类模型便宜但：不会推断上下文、爱编造、爱越权、爱灌水。规则就是用来抵消这些缺陷的。**把它当成一个不会思考只会执行的初级工，指令必须写到"傻子也能照做"。**

### 3.1 指令显式化
不要假定它懂项目背景。把背景、现状、已知雷区**全部写进 prompt**。它不会"结合我们之前说的"。

### 3.2 自包含 prompt
每个子代理的 prompt 必须**自我完整**：任务 + 文件路径 + 方法论 + 输出格式。禁止"如前述""按之前约定"。上下文断了它立刻胡来。

### 3.3 精确坐标
给**确切文件路径 + 行号**："审计 `ServerDetector.cs:502`" 而非 "看看检测那块"。廉价模型对模糊指代会自行脑补，脑补即失真。

### 3.4 只读默认
默认声明：**「只做只读审计，不修改任何文件」**。除非你明确让它写，否则禁止它动任何源码。这是防它"顺手重构"把你项目搞炸的底线。

### 3.5 禁止编造 / 声称须举证
**「不要写不存在的实现；声称修复必须给 `文件:行号` 证据。」** 这是 0d0488c 复审用血换来的规则——那次一个"系统性修复"commit 把不存在的缓存写进了 message。廉价模型尤甚此疾。

### 3.6 证据绑定
每条 finding 必须带 `文件:行号` + 违反的链 + 具体修复建议。**没有坐标的结论一律丢弃**。

### 3.7 输出克制
**「不灌水，表格为主，中文返回，精简有力。」** 廉价模型默认往长里写、往虚里写。用格式约束（表格 + 分级符号）把它压住。

### 3.8 信任但验证（Trust but verify）
**永远不要把子代理的"已完成/已修复"摘要当真。** 亲自 `git diff` 复核实际改动。历次审计里"声称修复 vs 实际修复"的落差，就是这条规则的来历。

### 3.9 边界明确
明确告诉它**不要碰什么**：「不要改 `App.xaml.cs` 的 DI 注册」「不要动 `MSMC.Tests` 除非我让你」「不要新建文件除非我要求」。

### 3.10 小步并行
大任务拆成**互不依赖的窄范围子代理并行**（如按模块分 6 个），不要把一个仓库丢给一个模型慢慢犁。窄范围 = 它犯错面小、你验证快。

### 3.11 收尾动作明确
告诉它最后**写哪个文件到哪**，还是**只回传结论不上盘**。廉价模型不知道"什么时候算完"，不写收尾它就原地飘。

### 3.12 中文输出
所有报告/结论用简体中文，与项目母语一致。

---

## 四、可复用：子代理 prompt 骨架（直接套用）

```
你是一名资深 .NET/WPF 代码审计员。任务：对本地仓库的 <模块> 做严格审计，
用户要求一针见血、不留情面。用「执行链/因果链/返回链」三链法逐行追溯。

## 源码位置（本地，用 Read/Bash/Grep，不要联网）
仓库根：/workspace/MSMC
模块目录：/workspace/MSMC/src/MSMC/Features/<模块>/
DI 注册核对：/workspace/MSMC/src/MSMC/App.xaml.cs（grep 验证每个注入接口已注册）
桥 handler：/workspace/MSMC/src/MSMC/Features/Shared/Views/MainWindow.xaml.cs

## 三链法（每条结论必须绑定证据）
- 执行链：路径是否真走通、async 是否正确、资源是否释放
- 因果链：依赖是否真存在、契约是否一致、前置条件是否满足
- 返回链：返回值/异常语义是否符合调用方预期、有无假成功

## 硬规则（违反即作废）
1. 只做只读审计，不修改任何文件
2. 每条 finding 必须带 文件:行号 + 违反的链 + 修复建议
3. 不要写不存在的实现；声称修复必须给 文件:行号 证据
4. 不灌水，表格为主，中文返回，精简有力

## 输出格式
模块评级(🔴/🟠/🟡) + 一句话总评
| 交互/代码点 | 文件名:行号 | 违反的链 | 问题 | 修复建议 |
```

---

## 五、一句话总纲

> **三链是尺子，诚实是底线，规则是缰绳。**
> 用三链量每一行，用诚实约束返回链（绝不假成功），用上面的作战规则把廉价模型拴在"只读 + 举证 + 不编造"的轨道上——它就不会造反。
